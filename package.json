{
  "name": "swisscows_dev",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": { 
    "test": "QASE_REPORT=1 QASE_RUN_NAME=UI xvfb-run  --project=${{ matrix.project }} --shard=${{ matrix.shardIndex }}/${{ matrix.shardTotal }} && npx playwright test api"
  },
  "keywords": [],
  "author": "Anton",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.40.1",
    "@types/node": "^20.8.10"
  },
  "dependencies": {
    "csv-parse": "^5.5.3",
    "dotenv": "^16.3.1",
    "playwright-qase-reporter": "^1.2.2",
    "rimraf": "^5.0.5"
  }
}
